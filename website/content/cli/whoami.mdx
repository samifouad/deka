    ---
    title: deka whoami
    description: Show current user and role
    ---

    # deka whoami

Show the current user and role on this instance.

```bash
deka whoami
```

## Options

- `-s, --server <url>` - Deka API URL (default: http://localhost:8520)

## Example output

```text
$ deka whoami
@samifouad (admin)
```
