    ---
    title: deka c exec
    description: Execute a command in a container
    ---

    # deka c exec

Execute a command in a running container.

```bash
deka c exec <container> [cmd...]
```

## Options

- `-i, --interactive` - Interactive mode (allocate TTY)

Example:

```bash
deka c exec edge ls -la
```
