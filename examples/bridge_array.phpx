import { json_encode } from 'json';

struct User {
    $id: int;
    $name: string;
}

$user = User { $id: 1, $name: 'Sami' };

$payload = [
    'id' => $user.id,
    'name' => $user.name,
];

echo json_encode($payload) . "\n";
