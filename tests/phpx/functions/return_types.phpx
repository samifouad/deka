/*
TEST: Return types + early return
Covers: typed return values and early return path.
*/

function get_name(bool $ok): string {
    if ($ok) {
        return 'Sami';
    }
    return 'Guest';
}

echo get_name(true) . "\n";
echo get_name(false) . "\n";
