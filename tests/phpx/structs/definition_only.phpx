/*
TEST: Struct definition parsing
Covers: struct declarations with fields + methods (no usage).
*/

struct Config {
    $host: string;
    $port: int;

    function url(): string {
        return $this.host . ':' . $this.port;
    }
}

echo "ok\n";
