<?php
$colors = ['red', 'green', 'blue'];
foreach ($colors as $index => $color) {
    echo "$index: $color\n";
}

$map = ['a' => 1, 'b' => 2];
foreach ($map as $key => $value) {
    echo "$key => $value\n";
}
