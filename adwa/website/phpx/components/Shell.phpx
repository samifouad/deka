interface ShellProps {
  $title: string
  $subtitle: string
  $children: any
}

export function Shell({ $title, $subtitle, $children }: ShellProps) {
  return <main class="phpxShell">
    <header>
      <h1>{$title}</h1>
      <p>{$subtitle}</p>
    </header>
    <section>{$children}</section>
  </main>
}
