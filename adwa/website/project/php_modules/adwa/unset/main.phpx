import { cmd_args, env_unset } from '../lib.phpx'
import { stdout } from 'io'

$args = cmd_args()
if (count($args) < 1) {
    stdout("unset: KEY required\n")
    return
}
foreach ($args as $key) {
    env_unset($key)
}
