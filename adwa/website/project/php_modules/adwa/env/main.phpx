import { env_all } from '../lib.phpx'
import { stdout } from 'io'

$env = env_all()
$keys = []
foreach ($env as $k => $_v) { $keys[] = '' . $k }
sort($keys)
foreach ($keys as $key) {
    stdout($key . '=' . $env[$key] . "\n")
}
